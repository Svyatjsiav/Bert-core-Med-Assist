## 📋 ОПИСАНИЕ ПРОЕКТА

- **Анализирует медицинские данные пациента** из JSON-файлов
- **Автоматически определяет диагноз** и подбирает соответствующие клинические рекомендации
- **Генерирует персонализированные схемы лечения** с учетом особенностей пациента
- **Использует BERT модели и Sentence Transformers** для обработки медицинских текстов
- **Поддерживает несколько заболеваний** в зависимости от исходных клинических ркомендаций

## 🏗️ АРХИТЕКТУРА СИСТЕМЫ

```
Bert-core Med Assist/
├── cli.py
├── core.py
├── requirements.txt
├── data/
│   └── Хронический вирусный гепатит С (ХВГС) параграфы.txt
└── ИБ/
    └── (здесь будут JSON файлы пациентов)
```

## 🎯 ПОРЯДОК ЗАПУСКА ПРОЕКТА
### УСТАНОВКИ НЕОБХОДИМОГО
#### 1. УСТАНОВКА PYTHON 3.8+

##### 🔹 Для Windows:
1. **Скачайте установщик** с [официального сайта Python](https://python.org/downloads/)
2. **Запустите установщик** и отметьте галочку ✅ **"Add Python to PATH"** (это критически важно!)
3. **Завершите установку** и перезапустите командную строку
4. **Проверьте установку**: откройте командную строку и введите:
   ```cmd
   python --version
   ```
   Должна отобразиться версия Python (например, `Python 3.11.0`)

##### 🔹 Для Linux (Ubuntu/Debian):
```bash
sudo apt update
sudo apt install python3 python3-pip
python3 --version
```

##### 🔹 Для Mac:
```bash
brew install python
python3 --version
```

#### 2. УСТАНОВКА НЕОБХОДИМЫХ БИБЛИОТЕК

В терминале/командной строке выполните:

##### 🔹 Для Linux/Mac:
```bash
# Переход в папку проекта
cd /путь/к/medical_assistant_bert

# Установка библиотек
pip3 install -r requirements.txt
```

##### 🔹 Для Windows:
```cmd
:: Переход в папку проекта
cd C:\путь\к\medical_assistant_bert

:: Установка библиотек
py -m pip install -r requirements.txt
```

### 🚀 ЗАПУСК ПРОЕКТА

#### Шаг 1: ПОДГОТОВКА ДАННЫХ

1. **Создайте структуру папок**:
```bash
# Linux/Mac
mkdir -p data ИБ

# Windows
md data ИБ
```

2. **Поместите клинические рекомендации** в папку `data/`:
   - Файл должен называться: `Хронический вирусный гепатит С (ХВГС) параграфы.txt`
   - Содержать актуальные клинические рекомендации по лечению гепатита С

#### Шаг 2: ЗАПУСК МЕДИЦИНСКОГО АССИСТЕНТА

```bash
# Интерактивный режим (рекомендуется)
python cli.py --interactive

# Или с указанием конкретного файла пациента
python cli.py --json-file путь/к/файлу_пациента.json

# Использование GPU (если доступен)
python cli.py --interactive --device cuda

# Использование конкретной BERT модели
python cli.py --interactive --model "sberbank-ai/ruT5-base"
```

### 🔍 ЧТО ПРОИСХОДИТ ПРИ ЗАПУСКЕ

1. **Загрузка BERT моделей** (автоматически скачиваются с HuggingFace)
2. **Открытие диалога выбора файла** пациента
3. **Анализ данных пациента** и извлечение диагноза
4. **Загрузка клинических рекомендаций** по гепатиту С
5. **Генерация эмбеддингов** для поиска релевантной информации
6. **Переход в режим вопрос-ответ** с пациентом

### 🕒 ВРЕМЯ ЗАПУСКА

- **Первый запуск**: 1-3 минуты (скачивание моделей с HuggingFace)
- **Последующие запуски**: 10-30 секунд (модели кэшируются локально)
- **Генерация ответов**: 2-10 секунд (зависит от устройства)

### 🛠️ РЕШЕНИЕ ЧАСТЫХ ПРОБЛЕМ

#### ❌ "python: command not found"
```bash
# Используйте python3 вместо python
python3 cli.py --interactive

# Или создайте алиас
alias python=python3
```

#### ❌ "ModuleNotFoundError: No module named 'transformers'"
```bash
# Установите библиотеку
pip install transformers
pip install sentence-transformers
pip install torch
```

#### ❌ "CUDA out of memory" (для GPU)
```bash
# Используйте CPU вместо GPU
python cli.py --interactive --device cpu

# Или используйте меньшую модель
python cli.py --model "DeepPavlov/rubert-base-cased" --interactive
```

#### ❌ "ConnectionError" при загрузке моделей
```bash
# Проверьте подключение к интернету
# Или используйте предварительно скачанные модели
# Можно установить модели оффлайн:
# 1. Скачайте модели с HuggingFace
# 2. Поместите в папку ~/.cache/huggingface/hub/
# 3. Укажите локальный путь в --model
```

#### ❌ "File not found: Хронический вирусный гепатит С (ХВГС) параграфы.txt"
```bash
# Убедитесь что файл существует в папке data/
# Создайте файл с клиническими рекомендациями
mkdir -p data
nano data/Хронический\ вирусный\ гепатит\ С\ \(ХВГС\)\ параграфы.txt
# Добавьте рекомендации по лечению гепатита С
```

### 📝 ПРИМЕР РАБОЧЕГО СЕАНСА

```bash
# Терминал 1 - Запуск ассистента
cd /путь/к/medical_assistant_bert
python3 cli.py --interactive

# После запуска:
# 1. Откроется окно выбора файла пациента
# 2. Выберите JSON файл с данными пациента
# 3. Система загрузит и проанализирует данные
# 4. Загрузит клинические рекомендации по гепатиту С
# 5. Перейдет в интерактивный режим

# Пример диалога:
# Вопрос: "Какое лечение рекомендуется для пациента с гепатитом С и циррозом печени?"
# Ответ: "Для пациентов с компенсированным циррозом печени рекомендуется..."
```

## 💻 ИСПОЛЬЗОВАНИЕ

### 🔸 Интерактивный режим (рекомендуется)
```bash
python cli.py --interactive
```
**Что происходит:**
1. Система предложит выбрать файл пациента через диалоговое окно
2. Автоматически загрузит клинические рекомендации по гепатиту С
3. Сгенерирует эмбеддинги для поиска
4. Перейдет в режим вопрос-ответ

### 🔸 Использование GPU для ускорения
```bash
python cli.py --interactive --device cuda
```

### 🔸 Использование другой BERT модели
```bash
python cli.py --interactive --model "sberbank-ai/ruT5-large"
```

## 📊 ФОРМАТ ДАННЫХ ПАЦИЕНТА

Система ожидает JSON файлы в формате:
```json
{
  "История болезни или наблюдений v.4": {
    "УникальныйID": {
      "Данные": {
        "Сведения при обращении": {
          "Клинический диагноз": {
            "Тип": "Текст",
            "Значение": "Хронический вирусный гепатит С"
          },
          "Возраст": {
            "Тип": "Число",
            "Значение": 45
          }
        }
      }
    }
  }
}
```

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### Безопасность:
- Система **не хранит медицинские данные** пользователей
- Все вычисления выполняются **локально** на вашем компьютере
- **Нет отправки данных** в облачные сервисы

### Ограничения:
1. **Специализация приведенного примера только на гепатите С** - система не обрабатывает другие заболевания без ваших самостоятельных изменений код
2. **Требует интернет** только для первой загрузки моделей
3. **Зависит от качества клинических рекомендаций** в файле параграфов
4. **Не заменяет врача** - все рекомендации должны быть проверены специалистом

### Рекомендации по использованию:
1. **Обновляйте клинические рекомендации** регулярно
2. **Проверяйте ответы системы** перед использованием
3. **Используйте актуальные данные пациентов**
4. **Сохраняйте резервные копии** важных рекомендаций

## 📞 ПОДДЕРЖКА

При возникновении проблем:
- Проверьте наличие файла с клиническими рекомендациями в папке `data/`
- Убедитесь что установлены все зависимости из `requirements.txt`
- Проверьте доступность интернета для загрузки моделей

## 🔮 ПЛАНЫ РАЗВИТИЯ

- [ ] Добавление поддержки обновления моделей
- [ ] Веб-интерфейс для удобства использования
- [ ] Кэширование эмбеддингов для ускорения работы
- [ ] Поддержка большего количества форматов данных пациентов
- [ ] Экспорт рекомендаций в PDF формат
- [ ] Интеграция с медицинскими базами данных (опционально)

---

**ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ**: Данная система предназначена для помощи медицинским работникам в подборе рекомендаций по лечению гепатита С и не заменяет профессиональное медицинское заключение. Все рекомендации должны быть проверены и подтверждены врачом-специалистом. Система использует только предоставленные клинические рекомендации и не имеет собственных медицинских знаний.